const success = '操作成功';
const unknownError = '未知的错误';

const defaultSuccessCode = 200;
const defaultErrorCode = 500;

export {
    success,
    unknownError,
    defaultSuccessCode,
    defaultErrorCode
};
